requests>=2.31
python-dotenv>=1.0
pydantic>=2.0
fastapi>=0.110
uvicorn>=0.27
google-genai>=1.0
# optional — Claude escalate only (disagreement / hard-reject); never default volume path
anthropic>=0.40
